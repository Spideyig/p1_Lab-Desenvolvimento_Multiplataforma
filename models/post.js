const db = require("/banco")

const Agendamentos = require  {
    nome:{
        type.sequelize.nome.STRING
    }
    telefone:{
        type.sequelize.telefone.STRING
    }
    origem:{
        type.sequelize.origem.STRING
    }
    data_contato:{
        type.sequelize.data_contato.STRING
    }
    observacao:{
        type.sequelize.observacao.TEXT
    }
}